# module = a file containing python code. May contain functions, classes, etc.
# used with modular programming, which is to separate a program into parts

import message as msg
# from message import hello,bye      # can also be written like this
# from message import *              # can also be written like this

# message.hello()
# message.bye()

# help("modules") # shows all the modules present in the python

msg.hello()
msg.bye()
